# crear db en una línea (sin módulos de localizacion)
docker-compose run --rm odoo click-odoo-initdb -n test -m sale_management,account,crm,website,stock,purchase,project,mail,contacts,calendar,mass_editing,web_responsive --no-demo --no-cache

# crear db en una línea (CON módulos de localizacion)
docker-compose run --rm odoo click-odoo-initdb -n test -m sale_management,account,crm,website,stock,purchase,project,mail,contacts,calendar,mass_editing,web_responsive,l10n_ar_bank,l10n_ar_account_withholding,l10n_ar_afipws_fe,l10n_ar_reports,account_financial_report,account_usability  --no-demo --no-cache

# loc ARG
,l10n_ar_bank,l10n_ar_account_withholding,l10n_ar_afipws_fe,l10n_ar_reports,account_financial_report,account_usability

# sin datos demo
--no-demo

# sin cache?
--no-cache

# español
el comando no admite este parámetro:
--load-language en_US,es_AR

# borrar la db
docker-compose run --rm odoo click-odoo-dropdb prod
